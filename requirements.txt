# WhatShouldICite - 科研写作引用建议 Agent

# 全局快捷键和系统集成（必需）
keyboard>=0.13.5          # 全局快捷键监听
pyperclip>=1.8.2           # 剪贴板操作
pyautogui>=0.9.54          # 模拟键盘操作（获取选中文本）

# Windows 专用（可选，但推荐）
pywin32>=306               # Windows API 访问（更好的文本获取）

# GUI 浮窗（必需）
# tkinter 通常随 Python 安装，无需额外安装

# LLM 集成（可选，如需使用 LLM 请安装）
openai>=1.0.0          # OpenAI API
anthropic>=0.3.0       # Anthropic Claude API
